set up error pages

set up redirects for randomcolor and typewriter and other projects

write short guide for deploying new projects

work out how to cache properly

add proxying feature

server {
    listen       8000;
    server_name tuuuune.localhost tuuune.com;
    root /var/www/llllll/sites/tuuune/public;
    error_page   403  = @proxy;
    try_files $uri index.html $uri/ @proxy;
    location @proxy {
        proxy_pass http://localhost:8080;
    }
}


http {

     open_file_cache max=1000 inactive=300s;
     open_file_cache_valid 360s;
     open_file_cache_min_uses 2;
     open_file_cache_errors off;
}

if ($http_cf_visitor ~ '{"scheme":"http"}') {
    return 301 https://$server_name$request_uri;
}